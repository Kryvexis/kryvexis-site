export default function OCRPage() {
  return (
    <main className="mx-auto max-w-6xl px-6 py-16">
      <h1 className="text-4xl font-semibold">OCR Document Capture</h1>
      <p className="mt-4 text-lg opacity-80">
        We convert invoices and documents into clean structured data (supplier, totals, line items) with review flags.
      </p>
    </main>
  );
}
